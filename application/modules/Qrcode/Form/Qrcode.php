<?php
/**
 * SocialEngine
 *
 * @package    qrcode
 */

/**
 * @package    qrcode
 */
class Qrcode_Form_Qrcode extends Engine_Form
{
	public function init()
	{
		/*$user_level = Engine_Api::_()->user()->getViewer()->level_id;
		$user = Engine_Api::_()->user()->getViewer();

		// Init form
		$this
		->setTitle('Create a new qrcode')
		->setDescription('Choose option on your screen to create qrcode')
		->setAttrib('name', 'qrcode_create');
		//Init Radio
		$formOptions = array('0'=>'Website','1' => 'Phone','2'=>'Status','3'=>'Contact');
		$this->addElement('Radio', 'field', array(
          'multiOptions' => $formOptions,

		));
	
		$this->addElement('Button', 'Submit', array(
        'label' => 'Generate QRCode',
        'type' => 'button',
        'id'=>'generate'
		
        ));

	}*/
	}
}
?>

